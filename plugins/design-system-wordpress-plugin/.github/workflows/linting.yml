name: Trigger Linting

on:
  pull_request:
    branches:
      - development
  workflow_dispatch:

jobs:
  trigger:
    permissions:
      contents: read
    uses: bcgov/des-git-workflows/.github/workflows/linting.yml@main
