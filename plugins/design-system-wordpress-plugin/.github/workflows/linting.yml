name: Trigger Linting

on:
  pull_request:
    branches:
      - development
  workflow_dispatch:

jobs:
  trigger:
    uses: bcgov/des-git-workflows/development/.github/workflows/linting.yml@feature/add-linting-workflow