/** Gutenberg stack uses React; pin version so eslint-plugin-react does not need a local `react` install. */
module.exports = {
    settings: {
        react: {
            version: '18.3',
        },
    },
};
