{
    "$schema": "https://json-schema.org/schema",
    "$id": "Plugin",
    "title": "Plugin",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Human-readable plugin name. Example: WordPress Search Plugin",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What name would you like to use?"
        },
        "wpEnvPort": {
            "type": "number",
            "description": "A free port to use for wp-env to serve on. Example: 9002",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "What port should wp-env serve on?"
        },
        "description": {
            "type": "string",
            "description": "(Optional) Block Plugin description. Example: This plugin provides a search block for WordPress.",
            "$default": {
                "$source": "argv",
                "index": 1
            },
            "x-prompt": "What description would you like to use?"
        }
    },
    "required": ["name", "wpEnvPort"]
}
