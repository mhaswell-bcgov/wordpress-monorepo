{
    "core": "WordPress/WordPress#6.8",
    "phpVersion": "7.4",
    "themes": [
        <% if(parentTheme !== "none") { %>
        ".",
        "bcgov/<%= parentTheme %>"
        <% } else { %>
        "."
        <% } %>
    ],
    "port": <%= wpEnvPort %>,
    "testsPort": <%= wpEnvPort + 1 %>,
    "lifecycleScripts": {
        "afterStart": "wp-env run tests-cli wp theme activate <%= slug %>"
    }
}
